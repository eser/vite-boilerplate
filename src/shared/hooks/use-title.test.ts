// import { renderHook } from "@testing-library/react-hooks";

// import { useTitle } from "./use-title.ts";

describe("useTitle", () => {
  it("should change page title", () => {
    //     renderHook(() => useTitle("testing title"));

    //     expect(document.title).toBe("testing title");
  });
});
